/**
 ******************************************************************************
 * @file           : main.c
 * @author         : Auto-generated by STM32CubeIDE
 * @brief          : Main program body
 ******************************************************************************
 * @attention
 *
 * Copyright (c) 2026 STMicroelectronics.
 * All rights reserved.
 *
 * This software is licensed under terms that can be found in the LICENSE file
 * in the root directory of this software component.
 * If no LICENSE file comes with this software, it is provided AS-IS.
 *
 ******************************************************************************
 */

#include <stdint.h>
#include<stdio.h>

#if !defined(__SOFT_FP__) && defined(__ARM_FP)
  #warning "FPU is not initialized, but the project is compiling for an FPU. Please initialize the FPU before use."
#endif

int main(void)
{
    /* Loop forever */

	/* Connecting the pins of Keypad Matrix on PA1 - PA7 */

	/* Step 1: Enabling Clock of GPIOA register */

	uint32_t RCC_BASE_ADDR = 0x40023800U;

	uint32_t* GPIOA_CLCK_EN = (uint32_t*)(RCC_BASE_ADDR +  0x30U);

	*GPIOA_CLCK_EN |= (1 << 0);

	/* Step 2: Configuring pin PA1-PA7 as input */

	uint32_t GPIOA_BASE_ADDR = 0x40020000U;

	uint32_t* GPIOA_MODE_REG = (uint32_t*)(GPIOA_BASE_ADDR + 0x00U);

	for(uint32_t i = 2; i <16; i++){

		*GPIOA_MODE_REG &= ~ (1<<i);      /* Reseting bits from PA1 - PA7*/

	}

	/* Setting pin PA1 - PA4 as Output*/
	*GPIOA_MODE_REG |= ((1<<2) | (1<<4) | (1<<6) | (1<<8));
	*GPIOA_MODE_REG &= ~((1<<3) | (1<<5) | (1<<7) | (1<<9));


	/* Setting PA5-PA7 as Input */
	*GPIOA_MODE_REG &= ~((1<<10) | (1<<11) | (1<<12) | (1<<13) | (1<<14) | (1<<15));


	/* Step 3: Configure Pull down resistor for all PA1-P7 pins */

	uint32_t* GPIO_PUPD_REG = (uint32_t*)(GPIOA_BASE_ADDR + 0x0CU);

	/* cONFIGURING PA1 - PA7 as pull down resistor */
	*GPIO_PUPD_REG |= ((1<<15) | (1<<13) | (1<<11));
	*GPIO_PUPD_REG |= ((1<<14) | (1<<12) | (1<<10));

	/* Step 4: Read pins PA1 - PA7 */

	uint32_t* GPIOA_IDR_REG = (uint32_t*)(GPIOA_BASE_ADDR + 0x10U);

	while(1){

		/* Row 1 and Column 1*/
		if(*GPIOA_IDR_REG & (1<<1) && *GPIOA_IDR_REG & (1<<5)){
			for(uint32_t i = 0; i < 100000 ; i++);
			printf("1\n");
			for(uint32_t i = 0; i < 100000 ; i++);
		}
		/* Row 1 and Column 2*/
		else if(*GPIOA_IDR_REG & (1 << 1) && *GPIOA_IDR_REG & (1<<6)){
			for(uint32_t i = 0; i < 100000 ; i++);
			printf("2\n");
			for(uint32_t i = 0; i < 100000 ; i++);
		}
		/* Row 1 and Column 3*/
		else if(*GPIOA_IDR_REG & (1 << 1) && *GPIOA_IDR_REG & (1<<7)){
			printf("3\n");
		}
		/* Row 2 and Column 1*/
		else if(*GPIOA_IDR_REG & (1 << 2) && *GPIOA_IDR_REG & (1<<5)){
			printf("4\n");
		}
		/* Row 2 and Column 2*/
		else if(*GPIOA_IDR_REG & (1 << 2) && *GPIOA_IDR_REG & (1<<6)){
			for(uint32_t i = 0; i < 100000 ; i++);
			printf("5\n");
			for(uint32_t i = 0; i < 100000 ; i++);
		}
		/* Row 2 and Column 3*/
		else if(*GPIOA_IDR_REG & (1 << 2) && *GPIOA_IDR_REG & (1<<7)){
			printf("6\n");
		}
		/* Row 3 and Column 1*/
		else if(*GPIOA_IDR_REG & (1 << 3) && *GPIOA_IDR_REG & (1<<5)){
			printf("7\n");
		}
		/* Row 3 and Column 2*/
		else if(*GPIOA_IDR_REG & (1 << 3) && *GPIOA_IDR_REG & (1<<6)){

			for(uint32_t i = 0; i < 100000 ; i++);
			printf("8\n");
			for(uint32_t i = 0; i < 100000 ; i++);
		}
		/* Row 3 and Column 3*/
		else if(*GPIOA_IDR_REG & (1 << 3) && *GPIOA_IDR_REG & (1<<7)){
			printf("9\n");
		}
		/* Row 4 and Column 1*/
		else if(*GPIOA_IDR_REG & (1 << 3) && *GPIOA_IDR_REG & (1<<5)){
			printf("*\n");
		}
		/* Row 4 and Column 2*/
		else if(*GPIOA_IDR_REG & (1 << 3) && *GPIOA_IDR_REG & (1<<6)){
			for(uint32_t i = 0;	i < 100000 ; i++);
			printf("0\n");
			for(uint32_t i = 0; i < 100000 ; i++);
		}
		/* Row 4 and Column 3*/
		else if(*GPIOA_IDR_REG & (1 << 3) && *GPIOA_IDR_REG & (1<<7)){
			printf("\n");
		}


	}


	for(;;);
}
