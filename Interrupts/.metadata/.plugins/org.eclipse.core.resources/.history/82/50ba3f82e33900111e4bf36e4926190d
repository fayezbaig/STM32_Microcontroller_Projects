/**
 ******************************************************************************
 * @file           : main.c
 * @author         : Auto-generated by STM32CubeIDE
 * @brief          : Main program body
 ******************************************************************************
 * @attention
 *
 * Copyright (c) 2026 STMicroelectronics.
 * All rights reserved.
 *
 * This software is licensed under terms that can be found in the LICENSE file
 * in the root directory of this software component.
 * If no LICENSE file comes with this software, it is provided AS-IS.
 *
 ******************************************************************************
 */

#include <stdint.h>
#include <stdio.h>

#if !defined(__SOFT_FP__) && defined(__ARM_FP)
  #warning "FPU is not initialized, but the project is compiling for an FPU. Please initialize the FPU before use."
#endif

/* Defining IRQ Number for EXTI0......page 375 */
#define EXTI0_IRQ_NO  6

int main(void)
{
    /* Loop forever */

	/* LED connected to PA4 and button is at PA0*/

	/* Step 1: Enabling Clock of GPIOA.....*/

	uint32_t RCC_BASE_ADDR = 0x40023800U;

	uint32_t* GPI0A_CLCK_EN = (uint32_t*)(RCC_BASE_ADDR + 0x30U);

	*GPI0A_CLCK_EN |= (1 << 0);

	/* Step 2: Configuring PA0 as input and PA4 as output pin */

	uint32_t GPIOA_BASE_ADDR = 0x40020000U;

	uint32_t* GPIOA_PORT_MOD_REG = (uint32_t*)(GPIOA_BASE_ADDR + 0x00U);

	*GPIOA_PORT_MOD_REG &= ~(3 << 0);    /* Reseting pin PA0 for input */

	*GPIOA_PORT_MOD_REG |= (1 << 8);
	*GPIOA_PORT_MOD_REG &= ~(1 << 9);

	/* Step 3: Configuring PA0 for Pull Down resistor */

	uint32_t* GPIOA_PUPD_REG = (uint32_t*)(GPIOA_BASE_ADDR + 0x0C);

	*GPIOA_PUPD_REG &= ~(3 << 0);   /* Clearing PA0 first */
	*GPIOA_PUPD_REG |= (2 << 0);	/* Configuring PA0 as Pull down */

	/* Step 4: Configuring IDR and ODR for PA0 and PA4 respectively */

	uint32_t* GPIOA_IDR = (uint32_t*)(GPIOA_BASE_ADDR + 0x10U);

	uint32_t* GPIOA_ODR = (uint32_t*)(GPIOA_BASE_ADDR + 0x14U);

	/* Step 5: Configuring SYSCFG for Interrupt */

	uint32_t SYSCFG_BASE_ADDR = 0x40013800U;

	uint32_t* GPIOA_SYSCFG_ADDR = (uint32_t*)(SYSCFG_BASE_ADDR + 0x08U);

	*GPIOA_SYSCFG_ADDR &= ~(15 << 0);    /* Reseting First 4 bits...... 15----> 1111*/


	/* Step 5: Enabling Interrupt on Pin PA0..Means Pin PA0 is allow to generate interrupt */

	uint32_t EXTI_BASE_ADDR = 0x40013C00U;

	uint32_t* GPIOA_EXTI_IMR = (uint32_t*)(EXTI_BASE_ADDR + 0x00U);

	*GPIOA_EXTI_IMR |= (1 << 0);

	/* Step 6: Configuring EXTI Trigger.....Select Rising Edge */


	*uint32_t GPIOA_EXTI_RSTR = (*uint32_t)(EXTI_BASE_ADDR +  0x08U);

	*GPIOA_EXTI_RSTR |= (1<<0);

	/* Step 7: NVIC enable.....IRQ0 handle IRQ 0 - 31....IRQ 1 handle 32 - 63 etc  */

	uint32_t* NVIC_ISER0 = (uint32_t*)0xE000E100U;   /* See page 219 of user generic guide */

	*NVIC_ISER0 |= (1 << EXTI0_IRQ_NO)


	for(;;);
}

/* Copied from Startup code*/
void EXTI0_IRQHandler(void){

	uint32_t GPIOA_BASE_ADDR = 0x40020000U;
	uint32_t EXTI_BASE_ADDR = 0x40013C00U;

	uint32_t* EXTI_PR = (uint32_t*)(EXTI_BASE_ADDR + 0x14U);
	uint32_t* GPIOA_ODR = (uint32_t*)(GPIOA_BASE_ADDR + 0x14U);

	if(*EXTI_PR & (1 << 0))
	    {
	        *GPIOA_ODR ^= (1 << 4);   /* Toggle LED */

	        *EXTI_PR |= (1 << 0);     /* Clear pending */
	    }

}
